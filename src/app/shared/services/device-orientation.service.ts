import { Injectable } from '@angular/core';

/**
 * Device orientation events are events that allow you to detect
 * a device's physical orientation, as well as allowing you to detect the device's motion.
 * 
 * @see https://developer.mozilla.org/en-US/docs/Web/API/Device_orientation_events
 */
@Injectable()
export class DeviceOrientationService {

  constructor() { }
}
