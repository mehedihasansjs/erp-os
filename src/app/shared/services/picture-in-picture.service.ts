import { Injectable } from '@angular/core';

/**
 * The Document Picture-in-Picture API makes it possible to open an always-on-top window
 * that can be populated with arbitrary HTML content — for example a video with custom controls
 * or a set of streams showing the participants of a video conference call.
 * It extends the earlier Picture-in-Picture API for <video>, which specifically enables an
 * HTML <video> element to be put into an always-on-top window.
 * 
 * @see https://developer.mozilla.org/en-US/docs/Web/API/Document_Picture-in-Picture_API
 */
@Injectable()
export class PictureInPictureService {

  constructor() { }
}
