import { Injectable } from '@angular/core';

/**
 * The Credential Management API enables a website to create, store,
 * and retrieve credentials. A credential is an item which enables a system
 * to make an authentication decision: for example, to decide whether
 * to sign a user into an account. We can think of it as a piece of evidence
 * that a user presents to a website to demonstrate that they really are the person they are claiming to be.
 * 
 * @see https://developer.mozilla.org/en-US/docs/Web/API/Credential_Management_API
 */
@Injectable()
export class CredentialsService {

  constructor() { }
}
