import { Injectable } from '@angular/core';

/**
 * The Barcode API provides the ability to read barcodes with an image from the Web.
 * It detects barcodes in real-time, on device, in any orientation.
 * It can also detect multiple barcodes at once.
 * The Barcode API can be used to scan a variety of barcode types.
 * Also this service allows you to generate barcodes
 * 
 * @see https://developer.mozilla.org/en-US/docs/Web/API/Barcode_Detection_API
 */
@Injectable()
export class BarcodeService {

  constructor() { }
}
