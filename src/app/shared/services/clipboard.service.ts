import { Injectable } from '@angular/core';

/**
 * The Clipboard API provides the ability to respond to clipboard commands
 * (cut, copy, and paste) as well as to asynchronously read from and write to the system clipboard.
 * 
 * @see https://developer.mozilla.org/en-US/docs/Web/API/Clipboard_API
 */
@Injectable()
export class ClipboardService {

  constructor() { }
}
